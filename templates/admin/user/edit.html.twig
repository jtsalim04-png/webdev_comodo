{% extends 'admin/base_admin.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0">Edit User: {{ user.firstName }} {{ user.lastName }}</h1>
    </div>

    <div class="card">
        <div class="card-body">
            {{ include('/admin/user/_form.html.twig', {'button_label': 'Update', 'back_path': path('app_user_index') }) }}
        </div>
    </div>
</div>
{% endblock %}

